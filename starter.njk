---
layout: base.njk
title: Starter
description: Your blank canvas - start building here
headerTagline: Starter
---


<style>
  .starter {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
    text-align: center;
    padding: var(--spacing-xl);
  }

  .starter__icon {
    font-size: 4rem;
    margin-bottom: var(--spacing-lg);
    opacity: 0.5;
  }

  .starter__title {
    font-size: var(--font-size-xxl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-md);
  }

  .starter__description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 500px;
    margin-bottom: var(--spacing-xl);
    line-height: var(--line-height-relaxed);
  }

  .starter__hint {
    background: var(--color-background-secondary);
    border: 1px dashed var(--color-border-default);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    max-width: 600px;
    width: 100%;
  }

  .starter__hint-title {
    font-weight: var(--font-weight-medium);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-sm);
  }

  .starter__hint-text {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    font-family: var(--font-family-mono);
  }

  .starter__actions {
    margin-top: var(--spacing-xl);
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
    justify-content: center;
  }
</style>


<div class="starter">
  <div class="starter__icon">🚀</div>
  <h2 class="starter__title">Hello, World</h2>
  <p class="starter__description">
    This is your blank canvas. Delete everything here and start building your app.
    The scaffold is ready — now make it yours.
  </p>

  <div class="starter__hint">
    <p class="starter__hint-title">Start here:</p>
    <p class="starter__hint-text">views/vanilla/starter.njk</p>
  </div>

  <div class="starter__actions">
    <a href="{{ '/getting-started/' | url }}" class="button button--primary">Setup Guide</a>
    <a href="{{ '/docs/' | url }}" class="button button--secondary">Read the Docs</a>
  </div>
</div>

{#
  This page intentionally has minimal JavaScript.
  Add your own app.js or import from core when you're ready:

  
  <script type="module">
    import { api, state, init } from '/core/index.js';
    // Your code here
  </script>
  
#}

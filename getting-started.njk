---
layout: base.njk
title: Get Started
description: Get started with Investiture - Quick setup guide
headerTagline: Get Started
---


<style>
  .steps {
    max-width: 800px;
    margin: 0 auto;
  }

  .step {
    background: var(--color-background-secondary);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
    position: relative;
  }

  .step__number {
    position: absolute;
    top: calc(-1 * var(--spacing-md));
    left: var(--spacing-lg);
    background: var(--color-accent-primary);
    color: var(--color-background-primary);
    width: 32px;
    height: 32px;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-sm);
  }

  .step__title {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-sm);
    margin-top: var(--spacing-xs);
  }

  .step__description {
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--spacing-md);
  }

  .step__code {
    background: var(--color-background-tertiary);
    border-radius: var(--radius-sm);
    padding: var(--spacing-md);
    font-family: var(--font-family-mono);
    font-size: var(--font-size-sm);
    color: var(--color-text-primary);
    overflow-x: auto;
    position: relative;
  }

  .step__code code {
    display: block;
    white-space: pre;
  }

  .step__note {
    margin-top: var(--spacing-md);
    padding: var(--spacing-sm) var(--spacing-md);
    background: rgba(78, 205, 196, 0.1);
    border-left: 3px solid var(--color-accent-secondary);
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  .next-steps {
    background: var(--color-background-secondary);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-md);
    padding: var(--spacing-xl);
    margin-top: var(--spacing-xxl);
  }

  .next-steps__title {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-accent-primary);
    margin-bottom: var(--spacing-lg);
  }

  .next-steps__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-md);
  }

  @media (min-width: 768px) {
    .next-steps__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .next-step-card {
    background: var(--color-background-tertiary);
    border-radius: var(--radius-sm);
    padding: var(--spacing-md);
  }

  .next-step-card__title {
    font-weight: var(--font-weight-medium);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-xs);
  }

  .next-step-card__description {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-sm);
  }

  .next-step-card__link {
    font-size: var(--font-size-sm);
    color: var(--color-accent-secondary);
  }

  .intro {
    text-align: center;
    margin-bottom: var(--spacing-xxl);
  }

  .intro__title {
    font-size: var(--font-size-xxl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-md);
  }

  .intro__description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }
</style>


<div class="intro">
  <h2 class="intro__title">Up and Running in 5 Minutes</h2>
  <p class="intro__description">
    Clone the repo, install dependencies, and start building your app with a clean, decoupled architecture.
  </p>
</div>

<div class="steps">
  <!-- Step 1 -->
  <div class="step">
    <div class="step__number">1</div>
    <h3 class="step__title">Clone the Repository</h3>
    <p class="step__description">
      Grab the scaffold from GitHub. You can clone it directly or use it as a template for your own repo.
    </p>
    <div class="step__code">
      <code>git clone git@github.com:erikaflowers/investiture.git my-app
cd my-app</code>
    </div>
    <div class="step__note">
      Replace <code>my-app</code> with your project name.
    </div>
  </div>

  <!-- Step 2 -->
  <div class="step">
    <div class="step__number">2</div>
    <h3 class="step__title">Install Dependencies</h3>
    <p class="step__description">
      Install the minimal dependencies. Right now it's just Sass for compiling SCSS.
    </p>
    <div class="step__code">
      <code>npm install</code>
    </div>
  </div>

  <!-- Step 3 -->
  <div class="step">
    <div class="step__number">3</div>
    <h3 class="step__title">Start the Dev Server</h3>
    <p class="step__description">
      Run the development server with live reload.
    </p>
    <div class="step__code">
      <code>npm run dev</code>
    </div>
    <div class="step__note">
      Opens at <a href="http://localhost:8080" target="_blank">http://localhost:8080</a>. Changes to templates and SCSS auto-reload.
    </div>
  </div>

  <!-- Step 4 -->
  <div class="step">
    <div class="step__number">4</div>
    <h3 class="step__title">Customize Your Brand</h3>
    <p class="step__description">
      Update the design tokens to match your brand. Edit <code>design-system/tokens/tokens.json</code> with your colors, typography, and spacing.
    </p>
    <div class="step__code">
      <code>{
  "color": {
    "accent": {
      "primary": "#your-brand-color",
      "secondary": "#your-secondary-color"
    }
  }
}</code>
    </div>
    <div class="step__note">
      Run <code>npm run build:tokens</code> to regenerate the SCSS and CSS files.
    </div>
  </div>

  <!-- Step 5 -->
  <div class="step">
    <div class="step__number">5</div>
    <h3 class="step__title">Update Your Content</h3>
    <p class="step__description">
      Edit the content files in <code>content/en/</code> to replace placeholder text with your app's copy.
    </p>
    <div class="step__code">
      <code>content/
├── en/
│   ├── common.json    # Button labels, states, auth text
│   └── errors.json    # Error messages</code>
    </div>
  </div>

  <!-- Step 6 -->
  <div class="step">
    <div class="step__number">6</div>
    <h3 class="step__title">Build Your App</h3>
    <p class="step__description">
      Start building! Use the <a href="{{ '/starter/' | url }}">Starter Page</a> as your blank canvas, or modify the existing views.
    </p>
    <div class="step__code">
      <code># Your app structure
core/          # Add your business logic here
views/vanilla/ # Build your UI here (or add React, Svelte, etc.)</code>
    </div>
  </div>
</div>

<!-- Next Steps -->
<div class="next-steps">
  <h3 class="next-steps__title">What's Next?</h3>
  <div class="next-steps__grid">
    <div class="next-step-card">
      <h4 class="next-step-card__title">Connect Real Services</h4>
      <p class="next-step-card__description">
        Wire up Supabase, Firebase, or your own backend in the service wrappers.
      </p>
      <a href="{{ '/docs/' | url }}#working-with-layers" class="next-step-card__link">Services Documentation →</a>
    </div>
    <div class="next-step-card">
      <h4 class="next-step-card__title">Add a Framework</h4>
      <p class="next-step-card__description">
        Create a React, Svelte, or Vue view alongside the vanilla implementation.
      </p>
      <a href="{{ '/docs/' | url }}#integration" class="next-step-card__link">Adding Frameworks →</a>
    </div>
    <div class="next-step-card">
      <h4 class="next-step-card__title">Add i18n Support</h4>
      <p class="next-step-card__description">
        Copy the <code>content/en/</code> folder to add new languages.
      </p>
      <a href="{{ '/docs/' | url }}#working-with-layers" class="next-step-card__link">Content Layer Docs →</a>
    </div>
    <div class="next-step-card">
      <h4 class="next-step-card__title">Client Theming</h4>
      <p class="next-step-card__description">
        Create custom themes for different clients or white-label deployments.
      </p>
      <a href="{{ '/docs/' | url }}#integration" class="next-step-card__link">Theming Guide →</a>
    </div>
  </div>
</div>
